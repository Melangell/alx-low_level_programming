#include "main.h"
/**
 * mul - multiples 2 integers
 * @a: first integer
 * @b: second integer
 * Return: the value of the multiplication of the 2 integers
 */
int mul(int a, int b)
{
	return (a * b);
}
