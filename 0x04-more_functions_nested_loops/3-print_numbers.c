#include "main.h"
/**
 * print_numbers - prints numbers from 0-9
 * Return: no return
 */
void print_numbers(void)
{
	int c;
	int _putchar(char c);

	if (c >= 48 && c <= 57)
		_putchar(c);
	_putchar('\n');
}
