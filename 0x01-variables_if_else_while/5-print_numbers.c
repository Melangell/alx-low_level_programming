#include <stdio.h>
/**
 * main - prints number of base 10
 * Return: 0 if successful
 */
int main(void)
{
	int n;

	for (n == '0'; n <= n % 10; n++)
{
	printf("%d", n);
}
	putchar('\n');
	return (0);
}
